# Documentation Index

Per-module documentation for the `ashboard-backend` project.

## Modules

- [Diary Entry](dairyentry/README.md) — diary entries, timeline events, food routine, tags, attachments
- [Expenses](expenses/README.md) — bank statement upload/parsing, transactions, expense categorisation
- [API](api/README.md) — top-level API entry, auth (JWT), schema/Swagger
- [Utilities](utils/README.md) — Vercel deployment, remote migrations, build script

## Live API docs

When the server is running:

- Swagger UI: `/api/schema/swagger-ui/` (custom dark theme)
- ReDoc: `/api/schema/redoc/`
- OpenAPI schema: `/api/schema/`
- Silk profiler: `/silk/`
